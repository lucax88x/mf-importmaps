{
	"name": "mf-importmaps",
	"private": true,
	"type": "module",
	"packageManager": "pnpm@10.27.0",
	"scripts": {
		"preinstall": "sh scripts/ensure-local-packages.sh",
		"build": "turbo build",
		"example:dev": "turbo dev",
		"example:typecheck": "turbo typecheck",
		"example:publish-local": "turbo publish:local",
		"example:types:emit": "turbo types:emit",
		"example:preview": "turbo preview",
		"example:docker-up": "docker compose -f example/docker-compose.yml up --build",
		"example:kill-ports": "tsx ./example/kill-ports.mts",
		"gh:ci": "act -W .github/workflows/ci.yml",
		"test:unit": "vitest run",
		"test:integration:install": "pnpm dlx playwright install --with-deps chromium",
		"test:integration": "pnpm run test:integration:dev && pnpm run test:integration:preview",
		"test:integration:dev": "MODE=dev pnpm run example:kill-ports && playwright test",
		"test:integration:preview": "MODE=preview pnpm run example:kill-ports && playwright test"
	},
	"pnpm": {
		"onlyBuiltDependencies": [
			"@typescript/native-preview"
		],
		"peerDependencyRules": {
			"allowedVersions": {
				"@vitejs/plugin-react>vite": "8"
			}
		}
	},
	"devDependencies": {
		"@playwright/test": "^1.50.1",
		"tsx": "^4.21.0",
		"turbo": "^2",
		"vitest": "^3.1.1",
		"zx": "^8.8.5"
	}
}
