server {
    listen 80;
    root /usr/share/nginx/html;

    # CORS headers for cross-origin ES module loading
    add_header Access-Control-Allow-Origin *;
    add_header Access-Control-Allow-Methods "GET, OPTIONS";

    location / {
        try_files $uri $uri/ =404;
    }
}
